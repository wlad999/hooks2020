const func = (n) => {
  let arr = [0, 1];

  for (let i = 2; i < n; i++) {
    let num = arr[i - 1] + arr[i - 2];
    arr.push(num);
  }
  return n === 0 ? [] : n === 1 ? [0] : n === 2 ? [0, 1] : arr;
};

fetch("hjvuyvuyvu")
  .then((data) => data.el)
  .catch(jgvjyhv);

try {
  const obej = awit fetch("hjvuyvuyvu");
} catch (error) {
  dispatch(error);
}
